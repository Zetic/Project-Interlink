import { WORLDGEN_PROTOCOL_VERSION, validateSyntheticRequest, validateTopologyRequest, type WorldgenCommand, type WorldgenErrorEvent, type WorldgenGeneratedSyntheticEvent, type WorldgenGeneratedTopologyEvent, type WorldgenSyntheticResult, type WorldgenTopologyResult } from './protocol.js';
interface WasmDiagnostic { generator_version(): number; stage_id(): string; stage_version(): number; stage_seed_hex(): string; width(): number; height(): number; sample_count(): bigint; minimum(): number; maximum(): number; mean(): number; field_hash_hex(): string; values(): Uint16Array; free(): void; }
interface WasmTopology { generator_version(): number; level(): number; sample_count(): number; edge_count(): number; face_count(): number; five_neighbor_count(): number; six_neighbor_count(): number; total_area_steradians(): number; minimum_area_steradians(): number; maximum_area_steradians(): number; mean_area_steradians(): number; area_coefficient_of_variation(): number; minimum_edge_arc_radians(): number; maximum_edge_arc_radians(): number; mean_edge_arc_radians(): number; edge_coefficient_of_variation(): number; topology_hash_hex(): string; positions(): Float64Array; faces(): Uint32Array; neighbor_offsets(): Uint32Array; neighbors(): Uint32Array; area_steradians(): Float64Array; birth_levels(): Uint8Array; parent_edges(): Uint32Array; free(): void; }
interface WorldgenWasmModule { default(): Promise<unknown>; worldgen_protocol_version(): number; worldgen_engine_version(): number; WasmWorldgenDiagnostic: new (seed: string, width: number, height: number) => WasmDiagnostic; WasmWorldgenTopology: new (level: number) => WasmTopology; }
interface WorldgenWorkerScope { addEventListener(type: 'message', listener: (event: MessageEvent<WorldgenCommand>) => void): void; postMessage(message: WorldgenGeneratedSyntheticEvent | WorldgenGeneratedTopologyEvent | WorldgenErrorEvent, transfer?: Transferable[]): void; }
const workerScope = self as unknown as WorldgenWorkerScope; let wasmModulePromise: Promise<WorldgenWasmModule> | null = null; function nowMs(): number { return globalThis.performance?.now?.() ?? Date.now(); }
async function loadWorldgenWasm(): Promise<WorldgenWasmModule> { if (wasmModulePromise) return wasmModulePromise; wasmModulePromise = (async () => { const moduleUrl = new URL('../../src/wasm-worldgen/interlink_worldgen_wasm.js', import.meta.url).href; let module: WorldgenWasmModule; try { module = await import(moduleUrl) as unknown as WorldgenWasmModule; } catch (error) { const detail = error instanceof Error ? error.message : String(error); throw new Error(`Planet Engine WASM package is not available. ${detail}`); } await module.default(); const actual = module.worldgen_protocol_version(); if (actual !== WORLDGEN_PROTOCOL_VERSION) throw new Error(`Planet Engine WASM protocol ${actual} does not match browser protocol ${WORLDGEN_PROTOCOL_VERSION}.`); return module; })(); return wasmModulePromise; }
async function generateSynthetic(command: Extract<WorldgenCommand, { type: 'generate-synthetic' }>): Promise<WorldgenSyntheticResult> { validateSyntheticRequest(command.payload); const module = await loadWorldgenWasm(); const startedAt = nowMs(); const output = new module.WasmWorldgenDiagnostic(command.payload.seed, command.payload.width, command.payload.height); try { const values = output.values(); return { engineVersion: output.generator_version(), width: output.width(), height: output.height(), values, statistics: { sampleCount: Number(output.sample_count()), minimum: output.minimum(), maximum: output.maximum(), mean: output.mean(), fieldHash: output.field_hash_hex() }, stage: { id: output.stage_id(), version: output.stage_version(), stageSeed: output.stage_seed_hex(), durationMs: Math.max(0, nowMs() - startedAt) } }; } finally { output.free(); } }
async function generateTopology(command: Extract<WorldgenCommand, { type: 'generate-topology' }>): Promise<WorldgenTopologyResult> { validateTopologyRequest(command.payload); const module = await loadWorldgenWasm(); const startedAt = nowMs(); const output = new module.WasmWorldgenTopology(command.payload.level); try { const positions = output.positions(); const faces = output.faces(); const neighborOffsets = output.neighbor_offsets(); const neighbors = output.neighbors(); const areaSteradians = output.area_steradians(); const birthLevels = output.birth_levels(); const parentEdges = output.parent_edges(); return { engineVersion: output.generator_version(), level: output.level(), durationMs: Math.max(0, nowMs() - startedAt), metrics: { sampleCount: output.sample_count(), edgeCount: output.edge_count(), faceCount: output.face_count(), fiveNeighborCount: output.five_neighbor_count(), sixNeighborCount: output.six_neighbor_count(), totalAreaSteradians: output.total_area_steradians(), minimumAreaSteradians: output.minimum_area_steradians(), maximumAreaSteradians: output.maximum_area_steradians(), meanAreaSteradians: output.mean_area_steradians(), areaCoefficientOfVariation: output.area_coefficient_of_variation(), minimumEdgeArcRadians: output.minimum_edge_arc_radians(), maximumEdgeArcRadians: output.maximum_edge_arc_radians(), meanEdgeArcRadians: output.mean_edge_arc_radians(), edgeCoefficientOfVariation: output.edge_coefficient_of_variation(), topologyHash: output.topology_hash_hex() }, positions, faces, neighborOffsets, neighbors, areaSteradians, birthLevels, parentEdges }; } finally { output.free(); } }
workerScope.addEventListener('message', async messageEvent => { const command = messageEvent.data; try { if (!command || command.protocolVersion !== WORLDGEN_PROTOCOL_VERSION) throw new Error(`Worldgen protocol must be ${WORLDGEN_PROTOCOL_VERSION}.`); if (command.type === 'generate-synthetic') { const result = await generateSynthetic(command); workerScope.postMessage({ protocolVersion: WORLDGEN_PROTOCOL_VERSION, requestId: command.requestId, type: 'generated-synthetic', payload: result }, [result.values.buffer]); return; } if (command.type === 'generate-topology') { const result = await generateTopology(command); workerScope.postMessage({ protocolVersion: WORLDGEN_PROTOCOL_VERSION, requestId: command.requestId, type: 'generated-topology', payload: result }, [result.positions.buffer, result.faces.buffer, result.neighborOffsets.buffer, result.neighbors.buffer, result.areaSteradians.buffer, result.birthLevels.buffer, result.parentEdges.buffer]); return; } throw new Error(`Unsupported worldgen command '${String((command as { type?: unknown }).type)}'.`); } catch (error) { const message = error instanceof Error ? error.message : String(error); workerScope.postMessage({ protocolVersion: WORLDGEN_PROTOCOL_VERSION, requestId: command?.requestId ?? -1, type: 'error', payload: { message } }); } });
