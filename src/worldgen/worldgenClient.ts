import { WORLDGEN_PROTOCOL_VERSION, validateSyntheticRequest, validateTopologyRequest, worldgenSyntheticCommand, worldgenTopologyCommand, type WorldgenEvent, type WorldgenSyntheticRequest, type WorldgenSyntheticResult, type WorldgenTopologyRequest, type WorldgenTopologyResult } from './protocol.js';
interface PendingRequest { resolve: (result: WorldgenSyntheticResult | WorldgenTopologyResult) => void; reject: (error: Error) => void; }
export interface WorldgenClient { generateSynthetic(request: WorldgenSyntheticRequest): Promise<WorldgenSyntheticResult>; generateTopology(request: WorldgenTopologyRequest): Promise<WorldgenTopologyResult>; dispose(): void; }
export function createWorldgenClient(): WorldgenClient { const worker = new Worker(new URL('./worldgenWorker.js', import.meta.url), { type: 'module' }); const pending = new Map<number, PendingRequest>(); let nextRequestId = 1; let disposed = false; function rejectAll(message: string): void { for (const request of pending.values()) request.reject(new Error(message)); pending.clear(); } worker.addEventListener('message', (event: MessageEvent<WorldgenEvent>) => { const message = event.data; if (!message || message.protocolVersion !== WORLDGEN_PROTOCOL_VERSION) return; const request = pending.get(message.requestId); if (!request) return; pending.delete(message.requestId); if (message.type === 'error') request.reject(new Error(message.payload.message)); else request.resolve(message.payload); }); worker.addEventListener('error', event => rejectAll(event.message || 'Planet Engine Worker failed.')); function request<T extends WorldgenSyntheticResult | WorldgenTopologyResult>(command: ReturnType<typeof worldgenSyntheticCommand> | ReturnType<typeof worldgenTopologyCommand>): Promise<T> { if (disposed) return Promise.reject(new Error('Planet Engine client is disposed.')); return new Promise((resolve, reject) => { pending.set(command.requestId, { resolve: result => resolve(result as T), reject }); worker.postMessage(command); }); } return { generateSynthetic(input) { validateSyntheticRequest(input); return request<WorldgenSyntheticResult>(worldgenSyntheticCommand(nextRequestId++, input)); }, generateTopology(input) { validateTopologyRequest(input); return request<WorldgenTopologyResult>(worldgenTopologyCommand(nextRequestId++, input)); }, dispose() { if (disposed) return; disposed = true; worker.terminate(); rejectAll('Planet Engine client was disposed.'); } }; }
